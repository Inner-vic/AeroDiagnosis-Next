# AeroDiagnosis v3 architecture memlog

2026-09-12 | constraint | Existing repository commit b318baa is a brownfield prototype and may be substantially refactored rather than only extended.
2026-09-12 | constraint | Deliverables must support a school research project, master's thesis, job interviews, resume content and GitHub presentation.
2026-09-12 | direction | User selected the fast path and will review the complete proposal after drafting.
2026-09-12 | decision | Bind the architecture to a hexagonal modular monolith so framework and infrastructure details cannot enter the domain model.
2026-09-12 | decision | Define Agent behavior through a durable typed LangGraph state machine rather than class names; this prevents fixed pipelines being presented as agents.
2026-09-12 | decision | Define one domain tool contract reused by LangGraph, FastAPI and MCP; this prevents semantic drift across adapters.
2026-09-12 | decision | Keep MCP as an external inbound adapter rather than an internal transport; this prevents loopback protocol coupling.
2026-09-12 | decision | Make evidence identity and provenance mandatory for retrieval, validation, reporting and evaluation; this prevents unverifiable citations.
2026-09-12 | decision | Verification failure must change the next action and parsing failure must not pass; this prevents ineffective retry loops and false assurances.
2026-09-12 | decision | Version every retrieval and prompt configuration and keep one fusion implementation; this prevents irreproducible experiments.
2026-09-12 | decision | Publish ingestion atomically by document version and content hash; this prevents partial and duplicate indexes.
2026-09-12 | decision | Make frozen-set offline evaluation a release gate; this prevents unsubstantiated claims in thesis, README and resume.
2026-09-12 | decision | Record operational events and evidence IDs but never private chain-of-thought; this binds observability and privacy.
2026-09-12 | decision | Anonymous demo access is read-only and all mutations require real authorization or remain disabled; this prevents fake authentication from being treated as security.
2026-09-12 | decision | Remove infrastructure without a caller, test and failure semantics; this prevents decorative Kafka and database dependencies.
2026-09-12 | version | Target seed verified on 2026-09-12: LangGraph 1.2.11, FastAPI 0.141.1, Pydantic 2.13.5, Neo4j 5.26.30 LTS, ChromaDB 1.5.9, Ragas 0.4.3 and OpenTelemetry SDK 1.44.0; compatibility remains a Phase 0 test obligation.
2026-09-12 | assumption | The public repository cannot redistribute proprietary engine manuals and will use authorized, synthetic or redacted samples.
2026-09-12 | question | The final thesis-scale dataset size and availability of a second domain annotator remain open and must be decided before the final experimental protocol is frozen.
2026-09-12 | event | architecture spine distilled and prepared for review.
2026-09-12 | constraint | Reviewer gate found that LangGraph Pydantic state validation occurs only for the first node input; explicit per-node boundary validation is required.
2026-09-12 | decision | SQLite manifest is the single publication authority across SQLite, Chroma and Neo4j; active-pointer reads and a reconciler prevent cross-store partial visibility.
2026-09-12 | decision | Fix document, version, chunk and evidence ID derivation and retain immutable referenced versions; this prevents independent stores from generating incompatible identities.
2026-09-12 | decision | Tool calls use a versioned request/result envelope with terminal statuses for success, error, timeout, cancellation and degradation; this prevents API, MCP and graph adapters from drifting.
2026-09-12 | decision | Durable execution is at-least-once with operation-id deduplication, monotonic budgets and explicit workflow/state versions; this prevents duplicate side effects and invalid resume.
2026-09-12 | decision | SQLite job leases, heartbeats, bounded backoff and dead-letter state govern the ingestion worker; this prevents duplicate ownership and infinite retry.
2026-09-12 | decision | Public demo mode is always anonymous read-only while local write mode is loopback plus an environment-injected operator token; this removes the prior ambiguous auth fork.
2026-09-12 | decision | Migrate by endpoint-level branch-by-abstraction with characterization tests and a mandatory final deletion of legacy code; this prevents both big-bang risk and permanent dual implementations.
2026-09-12 | decision | Evaluation policy fixes baseline, dataset hash, thresholds, statistics and infrastructure failure handling; this prevents contradictory release verdicts.
2026-09-12 | direction | Use the term knowledge-graph-enhanced RAG for the implemented approach; Microsoft GraphRAG remains related work unless its defining methods are actually implemented.
2026-09-12 | version | Reviewer-gate revision adds Python 3.13.x, MCP Python SDK 2.2.0, LangGraph SQLite Checkpoint 3.1.1, OTLP HTTP exporter 1.44.0 and FastAPI instrumentation 0.65b0; the latter is isolated because it remains beta.
2026-09-12 | event | reviewer-gate findings incorporated into architecture spine and upgrade plan.
2026-09-12 | decision | Use stdlib UUIDv4 for logical document identity and content-derived hashes below it; this avoids binding a separate UUIDv7 dependency.
2026-09-12 | decision | Derive side-effect operation IDs from a planner-issued stable call ID and canonical arguments, never from retry attempt; this preserves deduplication across retries and resumes.
2026-09-12 | decision | Freeze a content-addressed RunSnapshot at the first node and bind every read and resume to it; this prevents corpus, graph, prompt and model drift inside one run.
2026-09-12 | decision | Add a monotonic fencing token to every ingestion lease and require it on writes and publication; this prevents expired workers from advancing state.
2026-09-12 | decision | Derive trusted actor and execution budget only at inbound adapters, map one internal error registry to API/MCP and represent long operations as jobs; this prevents transport semantic drift.
2026-09-12 | decision | Prohibit raw prompt, evidence and tool payload data in telemetry, rebuild server trace identity and strip inbound baggage; this closes the observability trust boundary.
2026-09-12 | decision | Resumable graphs may register side-effect tools only when the adapter supports a provider idempotency key or deterministic reconciliation; this closes the external-effect versus local-ledger crash window.
2026-09-12 | decision | Every read tool declares all mutable data dependencies and RunSnapshot binds their resolved versions, including cases and parameter-analysis artifacts; this prevents hidden read-source drift.
2026-09-12 | decision | Assign a monotonic document revision and publish only the current desired revision with a compare-and-swap condition; this prevents concurrent valid ingestion jobs from racing the active pointer.
2026-09-12 | decision | Bind Git commit, container and lock digests plus tool implementation digests into RunSnapshot and refuse cross-build resume; this prevents behavior drift under an unchanged tool schema.
